@using Werewolves.Core.StateModels.Models
@using Werewolves.Core.StateModels.Models.Instructions

<HoldButton Label="@ClientStrings.Dashboard_ContinueButton"
			OnHoldComplete="Continue" />

@code {
	[Parameter, EditorRequired]
	public ConfirmationInstruction Instruction { get; set; } = default!;

	[Parameter]
	public EventCallback<ModeratorResponse> OnResponse { get; set; }

	private Task Continue()
	{
		return OnResponse.InvokeAsync(Instruction.CreateResponse());
	}
}
